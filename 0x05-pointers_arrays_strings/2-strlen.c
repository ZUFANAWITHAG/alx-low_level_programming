#include "main.h"

/**
 * _strlen - Function that returns the length of a string
 *
 * @s: this is thd input string
 *
 * Return: Length of the string
 */
int _strlen(char *s)
{
	int index;
	
	for (inde  = 0; s[index] != '\0' ; index++)
                ;	
	return (index);
}
