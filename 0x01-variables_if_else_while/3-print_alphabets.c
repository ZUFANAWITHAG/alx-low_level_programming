#include <stdio.h>
/**
 * main - main entry point
 * Return: Success
 *
 */

int main(void)
{
	char k = 'a';

	if (k <= 'z')
	{
		putchar(k);
	}
	else if (k >= 'z')
	{
		putchar(k);
	}
	putchar('\n');
	return (0);
}
