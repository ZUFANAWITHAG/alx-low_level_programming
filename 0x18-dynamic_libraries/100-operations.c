#include <stdio.h>

/**
 * add - adds a snd b
 * @a: first integer
 * @b: second integer
 * Return: addition
 */
int add(int a, int b)
{
	return (a + b);
}

/**
 * sub - subs a snd b
 * @a: first integer
 * @b: second integer
 * Return: ssbtraction
 */
int sub(int a, int b)
{
	return (a - b);
}
/**
 * mul - multiplies a snd b
 * @a: first integer
 * @b: second integer
 * Return: multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}

/**
 * div - division of  a snd b
 * @a: first integer
 * @b: second integer
 * Return: division
 */
int div(int a, int b)
{
	return (a / b);
}
/**
 * mod - mod of  a snd b
 * @a: first integer
 * @b: second integer
 * Return: modules
 */
int mod(int a, int b)
{
	return (a % b);
}
